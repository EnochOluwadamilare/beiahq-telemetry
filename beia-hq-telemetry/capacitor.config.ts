import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.enoch.beiahq',
  appName: 'BeiaHQ Telemetry',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
